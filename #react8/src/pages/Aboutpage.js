import React from "react";
import { Link } from "react-router-dom";

const Aboutpage = () => {
  return (
    <div>
      Aboutpage
      <button>
        <Link to={"/"}>Home</Link>
      </button>
    </div>
  );
};

export default Aboutpage;
